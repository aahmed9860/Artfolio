class User {
  final String uid;
  final String email;
  // Add more user properties as needed
  
  User({
    required this.uid,
    required this.email,
    // Initialize other properties here
  });
}
